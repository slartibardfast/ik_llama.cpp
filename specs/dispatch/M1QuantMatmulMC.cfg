\* TLC config for M1QuantMatmul.tla — POSITIVE test.
\* Mechanism = M1_FAITHFUL — an M1 kernel byte-identical to MMQ's per-column
\* output at EVERY real column (block_q8_1_mmq per M1MustUseMmqSrc1Quant,
\* MMQ's k-block float-scale order per M1MustMatchMmqScaleAccumOrder).
\* MaxBatch = 4 exercises the family (ne11 in 1..4 — np=1..4 / spec-decode).
\* All invariants must hold: substituting M1 for any ne11 <= K does not change
\* the decode token stream at any batch width.

CONSTANTS
    BaselineToken = "baseline"
    PerturbedToken = "perturbed"
    MaxStep = 5
    MaxBatch = 4
    Mechanism = "M1_FAITHFUL"

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

INVARIANTS
    TypeOK
    ObservationalEquivalence
    FaithfulByteIdentity
